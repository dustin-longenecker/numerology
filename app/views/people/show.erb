<div class="container">
  <div class="row">
    <h1><%= "#{@person.first_name} #{@person.last_name}" %></h1>
    <p>Birthdate: <%= "#{@person.birthdate}" %></p>
    <p><%= "#{@message}" %></p>
  </div>
</div>
